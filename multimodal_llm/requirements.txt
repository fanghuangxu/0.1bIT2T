# 多语言对话AI系统依赖

# 核心框架
torch>=2.0.0
transformers>=4.36.0
huggingface_hub>=0.20.0

# 量化支持
bitsandbytes>=0.41.0
accelerate>=0.25.0

# LoRA微调
peft>=0.7.0

# 数据处理
datasets>=2.14.0
numpy>=1.24.0
pandas>=2.0.0

# 日志和工具
tqdm>=4.65.0
rich>=13.0.0

# 可选: GGUF支持 (用于预量化模型)
# gguf>=0.1.0

# 开发工具 (可选)
pytest>=7.4.0
black>=23.0.0
ruff>=0.1.0

# 运行要求
# Python >= 3.8
# CUDA >= 11.0 (如需GPU支持)
